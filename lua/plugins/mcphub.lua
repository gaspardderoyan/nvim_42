return {
	-- "ravitemer/mcphub.nvim",
	-- dependencies = {
	-- 	"nvim-lua/plenary.nvim",
	-- },
	-- build = "npm install -g mcp-hub@latest", -- Installs `mcp-hub` node binary globally
	-- config = function()
	-- 	require("mcphub").setup({
	-- 		shutdown_delay = 3600000, -- 1 hour in milliseconds
	-- 		-- Set to true to automatically approve all MCP tool calls without user confirmation
	-- 		auto_approve = true,
	-- 		-- Set to true to allow LLMs (like CodeCompanion) to automatically start and stop MCP servers as needed
	-- 		auto_toggle_mcp_servers = true,
	-- 	})
	-- end,
}
